FROM node:18-alpine

WORKDIR /app

# 复制 package.json 和 pnpm lock file
COPY package*.json pnpm-lock.yaml* ./

# 安装 pnpm
RUN npm install -g pnpm

# 安装依赖
RUN pnpm install

# 复制源代码
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
