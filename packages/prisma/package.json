{
  "name": "@km/prisma",
  "version": "0.0.1",
  "description": "Prisma ORM package for the monorepo",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio",
    "reset": "prisma migrate reset",
    "build": "tsc"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "typescript": "^5.1.3"
  },
  "files": [
    "dist",
    "prisma"
  ]
}
