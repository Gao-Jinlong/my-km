{
  "name": "@my-km/prisma",
  "version": "0.0.1",
  "description": "Prisma ORM package for my-km monorepo",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "dev": "prisma generate",
    "generate": "prisma generate",
    "push": "prisma db push",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio",
    "reset": "prisma migrate reset",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@prisma/client": "^6.19.1"
  },
  "devDependencies": {
    "prisma": "^6.19.1",
    "typescript": "^5.7.3"
  },
  "files": [
    "dist",
    "prisma"
  ]
}
